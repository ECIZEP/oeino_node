type User {
    id: String,
    wechat: String,
    weibo: String,
    googleId: String,
    githubId: String,
    nickname: String,
    avatar: String,
    email: String,
    # 个性签名
    signature: String,
    intro: String,
    gender: Int,
    createTime: Int,
}

type Query {
    user: User
}